{
  "name": "dsh-remote-sandbox-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Production-grade remote-sandbox provider suite for DeepSeek Harness: crash-resilient, heartbeat-kept, tar-synced execution world over E2B.",
  "type": "module",
  "license": "MIT",
  "author": "weijiafu8",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' run build",
    "test": "vitest run",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.8",
    "esbuild": "^0.24.0",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0"
  }
}
